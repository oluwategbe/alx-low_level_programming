#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first input character
 * @b: second input character
 * Return: returns the multiplication of the integers
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
