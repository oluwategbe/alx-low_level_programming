#include "main.h"
/**
 * _abs - returns the absolute value of an integer
 * @n: input value
 *
 * Return: absolute value of an integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		reurn (n * -1);
	}
	else
	{
		return (n);
	}
}
