#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: first input
 * @b: second input
 *
 * Return: returns the result of the addition
 */
int add(int a, int b)
{
	return (a + b);
}
